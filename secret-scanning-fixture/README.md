# Secret-scanning fixture

This directory is a disposable test fixture for the repository secret-scanning setup.

Every credential in this fixture is fake and must never be used outside the test.
The decoy values are intentionally realistic-looking examples that should remain below
the scanner's detection threshold.

The AWS access key below is planted deliberately for history scanning tests.
